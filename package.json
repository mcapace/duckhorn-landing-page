{
  "name": "duckhorn-landing-page-root",
  "private": true,
  "scripts": {
    "dev": "cd duckhorn-ws && npm run dev",
    "build": "cd duckhorn-ws && npm run build",
    "start": "cd duckhorn-ws && npm run start",
    "lint": "cd duckhorn-ws && npm run lint"
  }
}
